[
  {
    "//": "list the user's workspaces",
    "method": "GET",
    "path": "/rest/api/2.0/workspaces",
    "origin": "https://${BITBUCKET_API}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "list the user's workspaces - AppRisk connector",
    "method": "GET",
    "path": "/workspaces",
    "origin": "https://${BITBUCKET_API}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "list the workspace's members",
    "method": "GET",
    "path": "/rest/api/2.0/workspaces/:workspace/members",
    "origin": "https://${BITBUCKET_API}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "list the workspace's repos",
    "method": "GET",
    "path": "/rest/api/2.0/workspaces/:workspace",
    "origin": "https://${BITBUCKET_API}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "list the workspace's repos",
    "method": "GET",
    "path": "/workspaces/:workspace",
    "origin": "https://${BITBUCKET_API}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "used to query for open pull requests by id",
    "method": "GET",
    "path": "/rest/api/2.0/repositories/:workspace/:repo/pullrequests",
    "origin": "https://${BITBUCKET}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "used to query for open pull requests by id - AppRisk",
    "method": "GET",
    "path": "/repositories/:workspace/:repo/pullrequests",
    "origin": "https://${BITBUCKET_API}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "used to query for open pull requests statuses by id",
    "method": "GET",
    "path": "/rest/api/2.0/repositories/:workspace/:repo/pullrequests/:pr/statuses",
    "origin": "https://${BITBUCKET}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "used to query for open pull requests statuses by id - AppRisk",
    "method": "GET",
    "path": "/repositories/:workspace/:repo/pullrequests/:pr/statuses",
    "origin": "https://${BITBUCKET_API}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "list commit for a given repo",
    "method": "GET",
    "path": "/rest/api/2.0/repositories/:workspace/:repo/commits",
    "origin": "https://${BITBUCKET}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "list repos for a given workspace",
    "method": "GET",
    "path": "/repositories/:workspace",
    "origin": "https://${BITBUCKET_API}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "list members for a given workspace - AppRisk",
    "method": "GET",
    "path": "/workspaces/:workspace/members",
    "origin": "https://${BITBUCKET_API}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "list commit for a given repo - AppRisk",
    "method": "GET",
    "path": "/repositories/:workspace/:repo/commits",
    "origin": "https://${BITBUCKET_API}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "list commit for a given repo",
    "method": "GET",
    "path": "/rest/api/2.0/repositories/:workspace/:repo/commits",
    "origin": "https://${BITBUCKET}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  },
  {
    "//": "get a specific commit diff stats identified by the commit hash or name of a branch",
    "method": "GET",
    "path": "/rest/api/2.0/repositories/:workspace/:repo/diffstat/commits/:sha",
    "origin": "https://${BITBUCKET}",
    "auth": {
      "scheme": "basic",
      "username": "${BITBUCKET_USERNAME}",
      "password": "${BITBUCKET_PASSWORD}"
    }
  }
]
