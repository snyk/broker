{
  "private": [
    {
      "path": "/echo-param/${param}",
      "method": "GET",
      "origin": "http://localhost:${originPort}"
    },

    {
      "path": "/echo-body/:param?",
      "method": "POST",
      "origin": "http://localhost:${originPort}"
    },

    {
      "path": "/echo-headers",
      "method": "POST",
      "origin": "http://localhost:${originPort}"
    }

  ],
  "public": [
    {
      "path": "/*",
      "method": "GET"
    },

    {
      "path": "/*",
      "method": "POST"
    }

  ]
}
