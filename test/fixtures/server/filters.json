{
  "private": [
    {
      "path": "/echo-param/${param}",
      "method": "GET",
      "origin": "http://localhost:${originPort}"
    },

    {
      "path": "/echo-body",
      "method": "POST",
      "origin": "http://localhost:${originPort}"
    }
  ],
  "public": [
    {
      "path": "/echo-param/${param}",
      "method": "GET"
    },

    {
      "path": "/echo-body",
      "method": "POST"
    }

  ]
}
