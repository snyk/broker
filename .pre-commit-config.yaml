repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.17.0
    hooks:
      - id: gitleaks
        stages: [ commit ]
  - repo: local
    language: system
    stages: [pre-commit]
    pass_filenames: true
    name: npm lint
    id: npm-lint
    entry: prettier --check
